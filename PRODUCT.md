# Product

## Register

product

## Users

使用 DeepSeek Harness 的开发者。他们希望启动桌面壳后尽快进入 dsh Web UI，不需要理解 Node、npm registry 或版本指针等运行环境细节。

## Product Purpose

为 dsh 提供小而可靠的桌面运行环境：托管 Node 与 dsh 版本、在失败时保留可用版本，并把维护操作收敛到首启、设置和托盘中。

## Brand Personality

克制、可靠、清晰。界面在后台处理复杂性，只在用户需要决策时提供简短、可操作的信息。

## Anti-references

避免营销页式的装饰、持续打断用户的通知、复杂的版本管理器，以及需要用户理解底层运行时的设置。

## Design Principles

- dsh Web UI 是主角，壳子只在维护状态时出现。
- 默认自动处理，失败时再提供恢复路径。
- 版本和错误信息准确、可验证、可回退。
- 非阻塞通知可被用户立即关闭，不强制下载或重启。
- 使用熟悉的桌面工具交互，避免为视觉效果创造新规则。

## Accessibility & Inclusion

支持键盘操作与清晰的无障碍标签；遵循系统的 `prefers-reduced-motion`，不把颜色作为唯一状态提示。
